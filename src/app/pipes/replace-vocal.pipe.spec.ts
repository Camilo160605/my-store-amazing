import { ReplaceVocalPipe } from './replace-vocal.pipe';

describe('ReplaceVocalPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceVocalPipe();
    expect(pipe).toBeTruthy();
  });
});
